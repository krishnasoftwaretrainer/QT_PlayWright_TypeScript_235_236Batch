"use strict";
let division = function (x, y) {
    let div = x / y;
    return div;
};
let divresult = division(20, 10);
console.log("Division of two numbers is: " + divresult);
//function multiplication(x:number,y:number):number
